# TICKET-0205: [assembler.py] Bare (non-tabled) photographs never wrapped in the required tr-caption-container structure
Status: Closed
Priority: High
Type: Bug
Created: 2026-07-05
Description: 4th test case (peru-2023) halted Phase 5 G2 Pass-2 on image_table_match: the source has 309 photos but only 4 Blogger caption tables -- 305 images use bare '<p><img/><br/>inline caption</p>' markup. The rev-18 workflow doc is explicit ('Each photograph must be in a tr-caption-container table', with the canonical template at doc lines 218-225), and the human-processed POSTED gold standard wrapped all 309 -- but the orchestrator never implemented the wrapping step because every prior test post (usa13, arsenalna, alaska) already had 100% table-wrapped images, leaving the gap invisible. The pipeline audited/required the structure without being able to produce it.
Steps to Reproduce: --full run on peru-2023-original; Phase 5 Pass-2 fails ['image_table_match'].
Notes: Implemented `assembler.wrap_bare_images(html)`, run first in `assemble()` (before corrections/style transforms). Zero-fabrication caption policy: the visible caption is the author's own INLINE caption -- the nodes after the first <br/> following the image in its block, MOVED (not copied) into the caption cell so any <a href> inside survives verbatim (G3) -- with the author's own alt text as fallback when no inline caption exists. An <a> already wrapping the image is kept around it inside the table; images already inside any table are untouched; unusual layouts (multi-image blocks, no enclosing block) are conservatively skipped. Verified against the real peru source: 305 wrapped -> 309/309 images in tables, media_inventory image_table_match=True, href diff removed=0 added=0 ok=True. Unit tests: test_wrap_bare_images (inline caption moved with its link, alt fallback, existing table untouched, wrapping <a> kept, href/inventory checks). NOTE the residual delta vs the human gold standard: POSTED also REWROTE most captions against the actual photos (only 1/308 equals the original alt) -- that is vision-level caption authoring, which is exactly the opt-in 1J audit's territory (TICKET-0167/0175/0204), not this deterministic wrap.
