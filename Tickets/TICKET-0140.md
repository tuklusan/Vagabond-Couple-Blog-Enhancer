# TICKET-0140: CR: full AI-communications transcript log in the run's output directory
Status: Open
Priority: Medium
Type: Improvement
Created: 2026-07-03
Description: User request (2026-07-03), scoped for the NEXT revision of the orchestrator, not this cycle: produce a complete, human-readable log file in the run's output directory that documents EVERY AI communication that occurred during the run -- every writer call and every reviewer call -- with a clear indicator of the SOURCE and TARGET of each communication (e.g. 'orchestrator -> writer (openrouter:auto)', 'writer -> orchestrator', 'orchestrator -> reviewer (deepseek:deepseek-v4-pro)', 'reviewer -> orchestrator'), in chronological order, covering the full run from Phase 1 through Phase 6. IMPLEMENTATION NOTES for whoever picks this up: (1) writer_client.chat() and reviewer_client.review_raw()/certify() are the two choke points where every LLM call already passes through -- likely the cleanest place to tap the prompt+response pairs without threading logging through every node; (2) orchestrator/state.py already has a run directory + JSONL event log (state.log()) -- extend that pattern with a dedicated e.g. ai_transcript.jsonl or ai_transcript.txt in the run dir, OR add a new state.log_ai_call(source, target, node_id, provider, prompt, response) method; (3) must capture which node/step the call belongs to (node.id from the sequencer) so the log is traceable back to Phase/Step names, not just raw prompt dumps; (4) consider a plain-text human-readable format (>>> ORCHESTRATOR -> WRITER [step6_first_body_paragraph] (openrouter:auto) ... <<< WRITER -> ORCHESTRATOR ...) alongside or instead of raw JSON, since the point is auditability by a human operator; (5) should probably be opt-in or always-on but clearly gitignored under Output/ like other run artifacts -- these transcripts may be large and contain full prompts. NOT implemented yet -- filed for the next revision per explicit user instruction to defer.
Steps to Reproduce: 
Notes: 
