# TICKET-0213: [sequencer.py] Mid-node provider/billing outage discards all per-item progress
Status: Closed
Priority: High
Type: Bug
Created: 2026-07-05
Description: Observed live (peru1v1 step13, 2026-07-05): the DeepSeek account ran out of credit (402 Payment Required) mid-way through the 193-pair separator loop. Two losses compounded: (1) `iterating_generative_node` writes its artifact only at node END, so every separator certified before the outage evaporated -- a rerun re-pays all the writer/reviewer calls; (2) the TICKET-0208 vision+research enrichment ran fresh at every node start, so a rerun also re-pays 193 VLM pair-inspections and the library/academic fetches. The run had to be killed to stop it burning rounds on guaranteed 402s.
Steps to Reproduce: Exhaust provider credit mid-node; kill; rerun; observe full re-payment.
Notes: Two incremental caches, both keyed on invariant item identity: (1) `gen_<node>_progress` artifact -- every CERTIFIED per-item outcome is checkpointed the moment it lands (`_item_cache_key`: subject/section_topic/pair_srcs only -- volatile fields like the research evidence text would defeat the cache); on rerun, cached outputs are reused without any provider call. (2) `step13_enrichment` artifact -- per-pair visible-description + evidence packet cached by pair srcs; reruns skip the VLM and research fetches for already-enriched pairs. Tests: test_enrichment_cached_across_reruns (exactly one VLM + one research call across two derivations) and test_iterating_node_progress_survives_outage (first run checkpoints; provider then RAISES on every call and the rerun still completes 1/1 from cache). OPERATOR ACTION REQUIRED to resume peru1v1: the DeepSeek account needs a credit top-up -- the pipeline cannot certify anything without its only reviewer. Once topped up, `--resume` continues loss-free.
