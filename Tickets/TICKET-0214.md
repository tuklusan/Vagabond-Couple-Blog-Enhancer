# TICKET-0214: [reviewer_client.py/writer_client.py] NVIDIA-hosted DeepSeek fallback -- same model, separate billing pool
Status: Closed
Priority: High
Type: Enhancement
Created: 2026-07-05
Description: The DeepSeek account ran out of credit mid-run (402 Payment Required), taking down the app reviewer, the writer's DeepSeek link, and the push gate simultaneously -- a single-billing-point failure for the pipeline's only reviewer. User question: can the same NIM API key serve DeepSeek? Verified yes: NVIDIA NIM hosts deepseek-ai/deepseek-v4-pro (and -v4-flash) on integrate.api.nvidia.com under the existing NVIDIA_API_KEY_CODING, and a live reviewer-style JSON certification call succeeded.
Steps to Reproduce: GET /v1/models on integrate.api.nvidia.com lists deepseek-ai/deepseek-v4-pro; chat completion returns clean JSON verdicts at temperature 0.
Notes: Implemented: (1) reviewer chain is now Claude -> DeepSeek (api.deepseek.com) -> NVIDIA-hosted DeepSeek ('deepseek-ai/' + REVIEWER_DEEPSEEK_MODEL via writer_client.call_nvidia) -> synthetic ESCALATE; model identity is preserved so review behavior matches the primary fallback. (2) writer chain gains a 'nim-deepseek-v4-pro' link between the DeepSeek API and nemotron, keeping the writer on the SAME model through a DeepSeek-account outage. Live-verified end-to-end: review_raw cascaded Claude-unusable -> 402 -> nim-deepseek and returned a correct verdict; the writer link echoed cleanly. NOT changed: the dev-review push gate keeps its documented role separation (env DEEPSEEK_API_KEY only); during a DeepSeek-account outage the gate fails open by design, and the operator can repoint it at NIM without code via its existing knobs (DEV_REVIEW_BASE_URL=https://integrate.api.nvidia.com/v1, DEV_REVIEW_DEEPSEEK_MODEL=deepseek-ai/deepseek-v4-pro, DEEPSEEK_API_KEY=<NIM key>). Note the app-side implication: the NIM pool now backs the reviewer, writer fallback, 1J vision audit, AND separator vision-nudge -- one key, watch its own quota.
