# TICKET-0161: Phase 5 Pass-1 reviewer truncated large posts at 200K chars (~1/4 of a real 826KB post), missing the ending entirely
Status: closed
Priority: Critical
Type: Improvement
Created: 2026-07-04
Description: User request: strengthen the final holistic reviewer to explicitly read the ENTIRE post (no truncation, including all image captions) as a fresh first-time reader, and explicitly call out timeline/geographic-route oscillation and content appearing after the post's own sign-off as smooth-read failures. While implementing this, found the underlying reason issues like TICKET-0160 (generated content landing after 'The End.') were not caught by Pass-1: _document_review() capped the reviewed HTML at html[:200000] CHARACTERS, with a comment claiming this was generous given deepseek-v4-pro's 1M-TOKEN context -- but 200,000 characters is only ~50K tokens, nowhere near 1M. On the 826KB alaska-cruise post, this covered under a quarter of the document; the reviewer never even reached the ending where the sign-off defect lived, so Pass-1 could never have caught it regardless of prompt wording. FIX: (1) raised the cap to 3,000,000 characters (comfortably covers any real post seen so far while still guarding a truly pathological multi-MB input); (2) rewrote the reviewer's system prompt with an explicit '18-year-old first-time reader, no skimming, no truncation, including captions' framing per the user's request, and named timeline-oscillation, geographic-route-oscillation, and post-continuing-after-its-own-sign-off as explicit smooth_read failure modes. Verify: re-run the alaska-cruise post and confirm Pass-1 actually reads to the end (check ai_transcript.txt for the reviewer's user-turn length) and would have caught the 0160-class defect if it recurred.
Steps to Reproduce: 
Notes: Fixed and verified: the reviewer now genuinely reads the full ~900KB document (previously truncated at ~1/4). Confirmed working -- it caught 7 of 9 occurrences of a repeated original caption phrase spread across the whole post (previously only found 3, all within the old 200K-char window), and correctly passed smooth_read after the 0160 ending fix. Deterministic test suites green.
