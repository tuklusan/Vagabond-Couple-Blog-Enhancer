# TICKET-0131: step1_title still forced road-trip format when origin/destination came back EMPTY (context-extraction non-determinism)
Status: closed
Priority: High
Type: Bug
Created: 2026-07-03
Description: 0127's single_location guard only triggered when origin was non-empty AND equal to destination. On a 3rd re-run of arsenalna1, context_extraction's LLM-derived route fallback returned origin='' and destination='' entirely (this run-to-run non-determinism itself is a separate, deeper issue -- see notes below), so single_location's bool(origin) check was False, the writer produced a broken literal title 'Overland', and the reviewer (unaware of any exception) kept demanding the road-trip format, looping to ESCALATE/abort. FIX: extracted _title_single_location(context) -- now true when origin OR destination is empty OR they're equal (not just the equal case); writer's fallback subject is dest-or-origin-or-post_title; reviewer's WRITING RULES criterion is now conditioned on the same single_location flag so it no longer insists on '[Origin] to [Destination] Overland via ...' when there's no real two-endpoint route. SEPARATE UNFIXED ISSUE noted for a future ticket: context_extraction's LLM-derived route (derive_route_from_prose, used when the source has no schema/route) is NON-DETERMINISTIC -- 3 consecutive arsenalna1 runs on the IDENTICAL input produced 3 different origin/destination/waypoints results (Arsenalna Metro Station Platforms Kyiv / Kyiv Kyiv Oblast+intermediate lobby waypoint / completely empty). This instability is a reliability gap worth its own investigation but is out of scope to fix here. Verify: re-run arsenalna1, confirm step1_title converges regardless of what context_extraction returns this time.
Steps to Reproduce: 
Notes: Fixed and verified via arsenalna1 re-runs; deterministic test suites green.
